spring:
  application:
    name: V-Beta-Server

# Cloud Run injects PORT. Local default remains 8080.
server:
  port: ${PORT:8080}

# Comma-separated list (e.g. http://localhost:3000,https://your-app.vercel.app)
app:
  cors:
    allowed-origins: ${CORS_ALLOWED_ORIGINS:http://localhost:3000}
