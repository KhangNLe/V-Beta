package app.VBeta.domain.model.discussions;

/**
 * Enumerates discussion item kinds persisted in {@link DiscussionRoot}.
 */
public enum DiscussionType {
    /** Text discussion comment entry. */
    COMMENT,
    /** Solution beta media entry. */
    BETA
}
