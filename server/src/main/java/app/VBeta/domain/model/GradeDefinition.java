package app.VBeta.domain.model;

/**
 * Enumerates supported climbing problem grades.
 */
public enum GradeDefinition {
    /** Beginner grade below V0. */
    VB,
    /** V0 grade. */
    V0,
    /** V1 grade. */
    V1,
    /** V2 grade. */
    V2,
    /** V3 grade. */
    V3,
    /** V4 grade. */
    V4,
    /** V5 grade. */
    V5,
    /** V6 grade. */
    V6,
    /** V7 grade. */
    V7,
    /** V8 grade. */
    V8,
    /** V9 grade. */
    V9,
    /** V10 grade. */
    V10,
    /** V11 grade. */
    V11,
    /** V12 grade. */
    V12,
    /** V13 grade. */
    V13,
    /** V14 grade. */
    V14,
    /** V15 grade. */
    V15,
    /** V16 grade. */
    V16,
    /** V17 grade. */
    V17
}
