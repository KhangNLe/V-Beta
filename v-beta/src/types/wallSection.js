/**
 * Wall section row from `GET /api/wall-sections` (table `Wall_Section` / entity JSON).
 *
 * @typedef {Object} WallSection
 * @property {number} wall_section_id
 * @property {string} wall_section_name
 * @property {string} [info] Description from DB `info` column.
 */

/** @typedef {WallSection[]} WallSectionList */

export {};
